---
- name: Install the MySQL packages
  apt:
    name: "{{ item }}"
    state: installed
    update_cache: yes
  with_items: "{{ ubuntu_mysql_pkgs }}"

- name: "Start the MySQL service"
  service: 
    enabled: true
    name: mysql
    state: started
  sudo: true
 
